let def_crate = require("./big_work.js");
let defautl_crate = def_crate(work_list);


function invert(work)
{
	let long = work[2];
	let height = work[0];
	work[0] = long;
	work[2] = height;
	return work;
}


function packing(work_list)
{
	crate = defautl_crate;




}
