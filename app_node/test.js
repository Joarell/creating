const cub = require('./app.js');

const work = [100, 5, 100];
console.log(cub.cubing(work));

// function cub(sizes)
// {
// 	return sizes[0] * sizes[1] * sizes[2]/6000
// }
//
// let a_Work = [100, 5, 100];
// console.log(Math.floor((cub(a_Work))*1000)/1000)
